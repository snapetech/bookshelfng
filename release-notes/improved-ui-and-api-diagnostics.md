---
category: fixed
audience: users, operators
area: diagnostics
action: none
breaking: false
---
BookshelfNG Docker builds now fail if UI assets are missing. Startup logs identify the build and runtime, and default debug logs include request IDs and command lookup details for UI and API failures.
