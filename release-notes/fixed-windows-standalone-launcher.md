---
category: fixed
audience: users, operators
area: standalone-install
action: none
breaking: false
---
Windows release archives and setup packages now include the Readarr.exe launcher expected by the install guide, so users can start BookshelfNG and operators can install it as a service without renaming files.
